# M3-c 性能基线(issue #12)

目标(spec「性能基线」):中端机(骨龙 7 系级别)稳定 ≥30fps,旗舰 ≥60fps;兼容 iOS Safari 15+ 与近两年 Android Chrome。手段全部落在渲染层——仿真核心一行未动(接缝 1 平价测试锁定,见下)。

## 三项手段

### 1. devicePixelRatio 封顶 2(仅小屏)

`game.js` 的 `tierPixelRatio()` 是像素比的唯一计算点(renderer 初始化、变档、resize 都走它):

- **小屏(移动目标)**:`min(devicePixelRatio, 当前档位上限)`,档位 0 上限为 2 —— 3x/4x 屏手机不再为肉眼无差别的细节付 9–16 倍片元开销。
- **桌面冻结断点(≥1024px)**:天花板恒为 1,即 #12 之前的渲染分辨率。桌面画面与帧率与改造前逐帧一致(档位 0–2 在桌面上完全等价);若不这样,HiDPI 桌面机会被抬到 4 倍片元开销,又因 27–50fps 滞回带永远回不去——桌面冻结验收条款不允许。

resize 时重新应用(跨显示器 / 浏览器缩放 / 跨断点都会改变结果)。

### 2. 请求粒子上限(只动表现,不动仿真)

- `src/render/perf.js` 的 **particle budget**:每个请求生成时向预算申请一个「可见槽」;超出上限的请求不创建 mesh(`src/entities/Request.js` 中 `this.mesh === null`),仿真照常运行。
- **聚合表现**:被聚合的流量按数量驱动互联网节点光环的脉冲(不透明度 + 缩放),高 RPS 压力仍然可见。
- 上限降低时**不驱逐**已有粒子——存量自然消亡,只有新请求被聚合,玩家无感。
- 全部请求粒子共享一份球体几何体;每请求仅保留材质(死亡闪烁需要独立颜色)。
- **接缝 1 平价测试** `src/render/particle-cap.parity.test.js`:同种子下,上限收紧到 1 与完全无上限,整局可观测轨迹(stats/经济/事件/健康度)必须逐项相等——粒子上限只影响视觉的验收条款。

### 3. 自动降阶(降阶档位)

`src/render/perf.js` 的 **quality governor** 采样真实(未经 timeScale 缩放的)帧间隔,EMA 平滑后:

- 持续低于 27fps 约 4 秒 → 降一档;
- 持续高于 50fps 足够久 → 升一档(每次降阶把回升所需时长翻倍,防抖);
- 每次变档后 6 秒冷却;27–50fps 之间是宽滞回带,永远保持现档。

档位表(`CONFIG.perf.tiers`,好 → 差):

| 档位 | pixelRatio 上限 | 阴影 | 材质 | 粒子上限 |
|---|---|---|---|---|
| 0 full | 2 | 开 | PBR (Standard) | 400 |
| 1 crisp | 1.5 | 开 | PBR | 300 |
| 2 baseline | 1 | 开 | PBR | 200 |
| 3 low | 1 | 关 | 简化 (Lambert) | 120 |
| 4 minimal | 0.75 | 关 | 简化 | 80 |

pixelRatio 一列是「档位上限」,实际值 = `min(设备天花板, 档位上限)`(见手段 1:桌面天花板为 1,因此桌面上档位 0–2 渲染完全相同,只有持续低于 27fps ——即改造前同样卡的机器——才会走到关阴影/简化材质,且 governor 给了它回升的路)。pixelRatio 先降、视觉特性后动:降阶总是先到达最便宜的分辨率,才碰画面特性。

当前档位记录在 `STATE.perf`({tier, tierName, fps, simpleMaterials}),每次变档 `console.info` 一条 `[perf]` 日志;变档在两帧之间应用,对玩家无打断(已知取舍:开关阴影需要整场景材质重编译,变档那一帧可能有一次顿挫,受 6 秒冷却限制不会频繁发生)。材质简化会把服务/互联网节点的 MeshStandardMaterial 换成便宜的 MeshLambertMaterial,回升时携带运行期状态(健康度着色、选中高亮)换回原材质。

## 验证(验收条款 1)

### 拟真验证数据(2026-07-18,浏览器设备拟真)

环境:Chromium(Playwright)+ **SwiftShader 软件渲染(无 GPU)**,CDP CPU 降速拟真设备档位;生产构建(`vite preview`);每档位真实游玩 90 秒:8 服务后期拓扑、锁定 40 RPS、默认流量分布(20% MALICIOUS),运行期间均经历恶意波次(DDoS);零页面错误。稳态取后 30 秒中位数。

| 拟真档位 | 视口/dpr/CPU 降速 | 渲染像素比 | 稳态 fps(中位) | 稳态最低 | 结算档位 | 档位轨迹 | 峰值并发请求 | 峰值聚合 |
|---|---|---|---|---|---|---|---|---|
| 中端机拟真 | 390×844 / 3 / 4x | 1 | 28 | 21 | 2 (baseline) | 0→1→2 | 153 | 0 |
| 旗舰拟真 | 428×926 / 3 / 1x | 1.5 | 30 | 27 | 1 (crisp) | 0→1 | 99 | 0 |
| 桌面(软渲染) | 1280×800 / 1 / 1x | 0.75 | 39 | 34 | 4 (minimal) | 0→1→2→3→4 | 101 | 30 |

结论与解读:

- **绝对帧率不代表真机**:SwiftShader 用 CPU 模拟 GPU,连"桌面"档也退到了软件渲染的地板(真实带 GPU 的桌面机不会触发降阶——桌面档位 0–2 渲染与 #12 之前逐帧一致)。数据验证的是**机制行为**:
- governor 在持续低帧率下逐档收敛、停在滞回带内且不振荡;中端拟真档被稳在设计地板(27–50fps 带)附近的 28fps,旗舰拟真档 30fps@1.5x 像素比。
- 桌面(软渲染)档走完全部 5 档并触发粒子聚合(101 并发中 30 个聚合、上限 80),仿真照常、无报错——极端弱机下的完整降阶路径可用。
- DDoS 波次在三档中都出现,期间游戏保持可交互,无一次崩溃或错误。

### 真机复核协议(可选,后续补充)

1. 打开 `<部署地址>/?perf=1` —— 左下角出现调试浮层:fps / 档位 / 粒子(可见+聚合)/ dpr / 阴影 / 材质。该浮层仅由 query 参数开启,不属于游戏 UI。
2. 生存模式正常游玩至后期(高 RPS + DDoS 波次,建议 ≥10 分钟)。
3. 记录:稳态 fps、最低 fps、结算档位(governor 停在哪一档)、聚合是否出现。
4. 控制台 `applyQualityTier(n)` 可手动锁档位对比帧率(governor 仍会自行调整)。

## 测试

- `src/render/perf.test.js` —— 粒子预算(申请/释放/纪元/收紧上限)与 governor(滞回、冷却、防抖回升、后台切换帧隙忽略)。
- `src/render/particle-cap.parity.test.js` —— 接缝 1:粒子上限不改变仿真结果。
- 桌面/移动渲染表现:人工回归(桌面冻结基准),配合 `?perf=1` 浮层。
