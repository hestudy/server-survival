# M3-d 用户故事 1–19 走查记录(issue #13)

日期:2026-07-18。范围:spec《移动端 Web 版 v1》用户故事 1–19(手机玩家视角全部条目)。

## 环境与方法

- Chromium(Playwright)设备拟真:390×844、dpr 3、触摸事件(CDP `Input.dispatchTouchEvent` 合成 tap / 单指拖拽 / 双指捏合 / 长按),iPhone UA;
- 生产构建(`npm run build` + `vite preview`),与 Pages 部署产物同构;
- 断言优先取可观测行为(`STATE` 仿真状态、DOM 可见性、画布截图差异),不窥探实现;
- 全程收集 `pageerror` 与 console error:**三个会话共计 0 条**;
- 旧存档场景使用真实 pre-refactor 存档 fixture(`src/sim/fixtures/save-v2-prerefactor.json`,8 服务 / 13 连线 / 已游玩 2m15s)。

与 M3-c(#12)一致的局限:拟真验证的是**机制行为**,绝对帧率、触觉震动、iOS Safari 真机表现仍需真机复核(见文末)。

## 逐条结果

| # | 用户故事(摘要) | 结果 | 证据 |
|---|---|---|---|
| 1 | 手机浏览器打开即进生存模式 | ✅ | 菜单关闭、`gameMode=survival`、运行中;小屏自动跳过教程 |
| 2 | 单指拖拽平移相机 | ✅ | 拖拽后画布截图变化(相机平移) |
| 3 | 双指捏合缩放 | ✅ | 捏合后画布截图变化(缩放生效) |
| 4 | tap 服务看实时详情 | ✅ | tap 防火墙 → 抽屉 Details 页展开,含名称/T1/健康度 100% |
| 5 | 工具栏选型后 tap 空位放置 | ✅ | 依次放置 waf、alb 成功(`svc_1`、`svc_2`) |
| 6 | 连线工具下两次 tap 建连线 | ✅ | `svc_1→svc_2` 连线建立 |
| 7 | 长按抬起后拖动换位 | ✅ | 长按 550ms 拖动,服务网格坐标 (-8,4)→(16,28) |
| 8 | 未抬起单指拖拽永远只平移 | ✅ | 平移后全部服务坐标不变、画布平移 |
| 9 | 顶部常驻紧凑状态条 | ✅ | 可见;开始后时间 0s→2s 跳动,资金/声誉/RPS 同步。注:新开局默认暂停(播放键闪烁提示),按播放后计时 |
| 10 | 财务/健康/详情收进底部抽屉 | ✅ | 三个标签页逐一切换正常 |
| 11 | DDoS/随机事件警告小屏醒目 | ✅ | 8 服务拓扑 3x 快进,状态条事件角标出现(30s 倒计时);DDoS 另有居中横幅 `#malicious-spike-indicator` |
| 12 | 工具栏横向滚动 + 触摸目标尺寸 | ✅ | 可横向滚动;按钮 64×64 CSS px(≥44 标准) |
| 13 | 中端机稳定 30fps | ✅* | governor 在线(`STATE.perf.fps≈60` 拟真环境);阈值数据见 `docs/perf/2026-07-18-m3c-performance-baseline.md`(中端拟真稳态 28fps 中位) |
| 14 | 性能不足自动降画质 | ✅* | 降阶档位机制在线(档位 0 full);降阶轨迹验证见 #12 拟真数据(0→1→2 等) |
| 15 | 暂停/加速触屏可用 | ✅ | tap 三键 → timeScale 0 / 1 / 3 |
| 16 | 修理与自动维修触屏可操作 | ✅ | 健康页内自动维修开关 tap 生效(false→true);修理按钮同面板呈现 |
| 17 | 存档跨版本升级依然有效 | ✅ | pre-refactor 存档注入 localStorage → Continue Game:8 服务(compute T2 / db T3)、13 连线、$507 全部还原;按播放继续游玩 5s:时间 135→140s、完成 134 请求、声誉 100%、零错误 |
| 18 | 小屏隐藏沙盒/战役/教程并提示 | ✅ | 三入口隐藏,菜单展示「用桌面浏览器游玩」提示 |
| 19 | 以系统语言显示(九语言) | ✅ | `locale: zh-CN` 上下文首次打开即中文(「开始生存模式」、标题「服务器:生存协议」、语言选择器= zh);由本次新增的 `navigator.languages` 检测实现,玩家手动选择仍优先 |

\* 13/14 的通过是机制层面(governor 运行、档位记录),量化阈值以 #12 的降速拟真数据为准。

## 走查中发现并已修复的问题(本 issue 内)

- **`data-i18n="in"` 键九语言全缺**(RPS 浪涌倒计时「×2.0 in 30s」),非英语界面裸露英文「in」→ 已补九语言并纳入自动检查;
- **`very high` / `very low` 键九语言全缺**(DB / Serverless 的维护费 tooltip 经 `t(upkeep.toLowerCase())` 取键)→ 已补九语言;
- **系统语言不自动检测**(故事 19 缺口:首次打开恒为英文)→ 新增 `pickLocale`(`src/pick-locale.js`),首启按 `navigator.languages` 匹配,显式选择(`game_locale`)始终优先。

## 留待真机复核项

- 13/14 的绝对帧率与降阶触发点(拟真为 SwiftShader 软渲染 + CPU 降速,见 #12 的说明);
- 长按抬起的触觉震动(`navigator.vibrate`,拟真环境无观测面);
- iOS Safari 15+ 实测(拟真为 Chromium 内核 + iPhone 视口/UA);
- 事件警告在真机小屏上的视觉「醒目」程度为主观项,拟真仅验证呈现面存在。
